上传到仓库
---
1. ###上传到github
    + 命令：`git push <url> <分支>`
    + 这个命令会将.本地的仓储上传到服务器上的某个分支上
    若第一次上传，则需要输入该服务器的所用账户的账号密码
2. ###得到远程分支
    + 命令：`git pull <url> <分支>`
    + 此时会把该分支从远程仓库下载下来 包括日志都可以查看 但需要先初始化自己的本地文件夹
    + 如果发生冲突 则需要先将自己的文件提交在本地仓库再pull就会出现标示
    + ！！！在我们上传的时候 本地与服务器文件不一致会导致冲突 所以在push时应先pull 在本地解决冲突后再push
3. ###得到远程仓库的文件
    + 命令：`git clone <url>`
    + 会将远程仓库的内容全部下载下来 与之前不同的是 这里只是单纯的将文件下载到本地
4. ###生成公钥
    + 命令：`ssh-keygen -t rsa -C <邮箱>`
    在C盘用户下生成ssh公钥 将他复制粘贴到远程仓库设置中 这样就可以不用验证账号密码了
5. pull与push的简写方式
    + 命令 ：`git remote add <变量名> <url>`
        - 这个命令可以将url赋给一个变量名 此后只需要使用变量名代替url即可
    + 命令 ：`git pull <变量名> -u <分支名>`
        - 在pull或push时加上-u 当前项目就会记录，此后push或pull时只需`git push`或`git pull`
    + 注意！ 简写方式只能存在于本地的该项目中