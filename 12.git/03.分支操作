分支操作
---
1. ###创建分支
    + 命令：`git branch <分支名>`
2. ###查看分支
    + 命令：`git branch`
    + 主分支为master 当前在哪个分支 分支名前会有一个*
3. ###切换分支
    + 命令：`git checkout <分支名>`
    + 一旦创建分支并切换到其分支后 提交代码都会提交到该分支上
    不会影响主分支的内容 而且查看日志会从创建分支时开始发生变化
4. ###合并分支
    + 命令：`git merge <分支名>`
    + 此时会把当前分支与目标分支合并
    + 合并时如果有冲突 会失败并且git会在代码中提示 此时需要手动处理 处理完后再提交
5. ###删除分支
    + 命令：`git branch -d <分支名>`
    + 注意：删除分支时必须不在当前分支下，如果在则删除不会成功
